# Cursor Customizations

This directory mirrors the repository's Claude-native AI tooling (canonical
source in `.claude/`) via symlinks so it is discoverable in Cursor. Edit the
`.claude/` sources, not this directory.
