[tool:pytest]
timeout = 600
timeout_method = thread
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    flaky: marks tests that retry via pytest-retry to absorb known process-shutdown flakiness
