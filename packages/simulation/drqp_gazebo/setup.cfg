[develop]
script_dir=$base/lib/drqp_gazebo
[install]
install_scripts=$base/lib/drqp_gazebo

[tool:pytest]
timeout = 180
timeout_method = thread
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
