[build_scripts]
executable=/usr/bin/env python3

[install]
install_scripts=$base/lib/drqp_robot_mcp

[tool:pytest]
timeout = 600
timeout_method = thread
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
