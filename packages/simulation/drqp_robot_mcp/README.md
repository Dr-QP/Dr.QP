# drqp_robot_mcp

ROS 2 package for the Dr.QP robot MCP server in the simulation workspace.

This package is being migrated incrementally from `py_packages/drqp_robot_mcp`.
During migration, the editable workspace source now points at this package.